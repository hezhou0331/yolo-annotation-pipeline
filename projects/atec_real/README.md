# ATEC真实项目

提交到Git的内容只有类别配置和Manifest。以下目录均为本地工作区：

- `data/`：RGB-D输入、关键Mask和跟踪Mask；
- `datasets/`：YOLO11-seg导出结果；
- `assets/`：CAD和Mesh；
- `reports/`：运行报告。

这些目录不会上传GitHub。新电脑需要自行恢复数据，或运行项目初始化/采集命令重新创建。
